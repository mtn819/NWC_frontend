import React from 'react';
import "./CaptionedButton.css";

function CaptionedButton({img, text}) {
  return (
    <div className="CaptionedButton">
      
    </div>
  )
}

export default CaptionedButton

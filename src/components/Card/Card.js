import React from 'react'
import "./Card.css";

function Card({header, content}) {
    return (
        <div className="card">
            <h2>{header}</h2>
            
        </div>
    )
}

export default Card
